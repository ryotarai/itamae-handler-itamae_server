require 'spec_helper'

describe Itamae::Handler::ItamaeServer do
  it 'has a version number' do
    expect(Itamae::Handler::ItamaeServer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
